# Local-only development identity. Production requests must use the configured auth provider.
VITE_DEMO_USER_ID=demo-user

# Provision D1 before remote deployment, then replace the placeholder database_id in wrangler.jsonc.
CLOUDFLARE_ACCOUNT_ID=
